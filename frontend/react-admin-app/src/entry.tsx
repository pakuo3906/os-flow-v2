import React from "react";
import ReactDOM from "react-dom/client";
import ManifestAdminApp from "./manifestAdminApp";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <ManifestAdminApp />
  </React.StrictMode>,
);
